{
  "name": "educonnect-frontend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "EduConnect SaaS — React frontend wired to the EduConnect REST API",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check:csv": "node scripts/check-csv.mjs",
    "check:urdu": "node scripts/check-urdu.mjs",
    "check:ui": "node scripts/check-ui.mjs",
    "check": "npm run check:csv && npm run check:urdu && npm run check:ui"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11"
  }
}
