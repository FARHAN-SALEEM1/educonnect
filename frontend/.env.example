# Base URL for the EduConnect API.
# Left as "/api", Vite's dev proxy forwards to http://localhost:5000 (see vite.config.js).
# For a deployed build, set the full URL, e.g. https://api.yourschool.com/api
VITE_API_URL=/api
