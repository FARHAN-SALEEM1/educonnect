# Dependencies
node_modules/

# Secrets — never commit these.
# .env holds the database password and JWT signing keys.
.env
.env.local
.env.*.local
!.env.example

# Build output
dist/
build/
frontend/dist/

# Logs
*.log
npm-debug.log*
yarn-error.log*

# Editors / OS
.idea/
.vscode/
# Machine-specific Claude Code settings
.claude/settings.local.json
.DS_Store
Thumbs.db

# Test & coverage
coverage/

# Local scratch
*.tmp
_repair.mjs

# Stray browser-preview artefacts
preview-screenshot.png

# Database dumps — these contain real student personal data.
# Never commit them; keep them encrypted and off the repo host.
backups/
*.dump
*.sql.gz

# Captured webhook deliveries (SAFEPAY_CAPTURE_WEBHOOKS). Sandbox diagnostic
# only: each line holds a payer's details and a valid gateway signature.
# Delete them once the payload has been studied.
*.webhooks.jsonl
